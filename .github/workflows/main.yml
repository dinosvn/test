on:
  push:
    tags:
      - '*'

name: Test, Build and Release apk

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-java@v1
        with:
          java-version: '12.x'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '2.x.x'
      - run: flutter pub get
      - run: flutter test
      - run: flutter build apk --release --split-per-abi

  release:
    name: Create a Release
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Create Tag
        run: git tag -a v${{ github.run_number }} -m "Release version v${{ github.run_number }}"
      - name: Push Tag
        run: git push origin v${{ github.run_number }}
      - name: Create a Release APK
        uses: softprops/action-gh-release@v1
        with:
          files: build/app/outputs/apk/release/*.apk
        env:
          GITHUB_TOKEN: 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
